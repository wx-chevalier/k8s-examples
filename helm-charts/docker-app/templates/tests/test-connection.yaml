apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yourbiz-static.fullname" . }}-test-connection"
  labels:
{{ include "yourbiz-static.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "yourbiz-static.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
